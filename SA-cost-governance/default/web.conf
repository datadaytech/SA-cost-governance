[settings]
# Disable the confirmation prompt for search commands requiring write access (outputlookup, etc.)
# This allows governance automation to run without user intervention
enableSearchCommandWriteAccessWarning = false
