# PII Findings Lookup
[pii_findings_lookup]
filename = pii_findings.csv
max_matches = 1000
min_matches = 0

# PII Whitelist Lookup
[pii_whitelist_lookup]
filename = pii_whitelist.csv
max_matches = 1000
min_matches = 0

# PII Patterns Lookup
[pii_patterns_lookup]
filename = pii_patterns.csv
max_matches = 100
min_matches = 0

# PII Audit Log Lookup
[pii_audit_log_lookup]
filename = pii_audit_log.csv
max_matches = 10000
min_matches = 0

# PII Settings Lookup
[pii_settings_lookup]
filename = pii_settings.csv
max_matches = 100
min_matches = 0

# PII Scan History Lookup
[pii_scan_history_lookup]
filename = pii_scan_history.csv
max_matches = 1000
min_matches = 0

# PII Scan Cache - For performance
[pii_scan_cache_lookup]
filename = pii_scan_cache.csv
max_matches = 10000
min_matches = 0
