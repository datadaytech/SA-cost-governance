#
# Splunk TA for Enhanced Schedule Builder
#

[install]
is_configured = true
state = enabled
build = 7

[package]
id = TA-schedule-builder
check_for_updates = false

[launcher]
author = DataDay Tech
description = Enhanced cron expression builder for Splunk alerts and reports. Provides granular control over scheduling with multi-select for minutes (0-59), hours (0-23), and days of week.
version = 1.0.0

[ui]
is_visible = true
label = Schedule Builder
